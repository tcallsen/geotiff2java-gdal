<?xml version="1.0" encoding="UTF-8"?>
<project>
  <modelVersion>4.0.0</modelVersion>
  <packaging>jar</packaging>
  <name>geotiff2java_gdal</name>
  <artifactId>geotiff2java_gdal</artifactId>
  <groupId>me.callsen.taylor</groupId>
  <version>1.0.0-SNAPSHOT</version>

  <description>Java 11 project demonstrating how to read values from GeoTIFF files using the GDAL Java bindings.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <main.class>me.callsen.taylor.geotiff2java_gdal.App</main.class>
    <maven.compiler.target>11</maven.compiler.target>
    <maven.compiler.source>11</maven.compiler.source>
  </properties>

  <dependencies>

    <!-- GDAL - requires external gdal library and bindings to be available -->
    <!-- installed optionally, pointing to system localtion of gdal.jar -->
    <!-- (systemPath may be different based on OS / GDAL build steps) -->
    <dependency>
      <groupId>org.gdal</groupId>
      <artifactId>gdal</artifactId>
      <version>3.0.4</version>
      <scope>system</scope>
      <systemPath>/usr/share/java/gdal.jar</systemPath>
    </dependency>

  </dependencies>

</project>